<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.v.graphics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".xfermode.MyActivity"/>
        <activity android:name=".porterDuff.PorterDuffActivity"/>
        <activity android:name="com.v.demo.shadow.ShadowActivity"/>
        <activity android:name="com.v.demo.shadow.FirstActivity"/>
        <activity android:name="com.v.demo.shadow.SecondActivity"/>
    </application>

</manifest>